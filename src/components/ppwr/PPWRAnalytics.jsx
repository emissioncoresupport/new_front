import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function PPWRAnalytics() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>PPWR Analytics</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-slate-600">Packaging waste reduction analytics</p>
      </CardContent>
    </Card>
  );
}